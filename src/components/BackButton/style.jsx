import styled from '@emotion/styled';

export const BackButtonWrapper = styled.div`
  color: var(--color-text-gray);
  cursor: pointer;
  margin-bottom: 15px;
  font-size: 0.9rem;
  display: flex;
  align-items: center;
  gap: 0.2rem;
`;
